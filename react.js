import React, { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import "./styles.css";

import App from "./App";

const root = createRoot(document.getElementById("root"));
root.render(
  <StrictMode>
    <App />
  </StrictMode>
);

//  ----------------------     --------------------------    -------------------------------   ---------------------------------

// note cho em trong quá trình làm.
// Đọc kĩ từng lỗi xảy ra.
// Dành nhiều thời gian để đọc hiểu lỗi.

// Đừng cố gắng làm cho xong

// à quên. đọc kĩ cách ngta đặt tên biến. học thuộc đi cũng được. sau 99% thời gian ko có tiếng việt đâu

// học luôn quy tắc đặt tên biến tên hàm tên nhánh trên git.

// học kĩ phần HTML. tag nào dành cho cái nào
